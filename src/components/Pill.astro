---
interface Props {
	href?: string;
}

const { href } = Astro.props;
---

<a
	href={href}
	target="_blank"
	rel="noopener noreferrer"
	class="flex opacity-70 items-center gap-x-2 bg-white/15 py-1 px-4 rounded-full hover:scale-110 hover:opacity-100 transition"
>
	<slot />
</a>
